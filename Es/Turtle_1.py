import turtle 
disegno = turtle.Turtle()
distanza= []
angolo = []
movimento = "continua"
while (ris !="exit") :
    
    
    stringa =input("inserisci la direzione")
    ang= ("inserisci un angolo di rotazione")
    dist=("inserisci un distanza")
    if(movimento== stringa) :
        if(ang== angolo) :
            if(dist== distanza ):
                if(stringa== 5): 
                    if(stringa== "avanti"):
                        turtle.forward
                break


disegno.screen.mainloop()


